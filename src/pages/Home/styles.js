import { makeStyles } from '@material-ui/core/styles';

const style = makeStyles(() => ({
    home_container: {
      paddingTop: 36,
      display: 'flex',
      flexWrap: 'wrap'
    },
  }));

export default style 