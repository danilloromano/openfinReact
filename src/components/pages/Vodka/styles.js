import { makeStyles } from '@material-ui/core/styles';

const style = makeStyles(() => ({
    vodka_container: {
      paddingTop: 30,
      display: 'flex',
      width: '100%',
      height: '100%',
      flexWrap: 'wrap'
    },
  }));

export default style 