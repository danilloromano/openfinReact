import { makeStyles } from '@material-ui/core/styles';

const style = makeStyles((theme) => ({
    home_container: {
      paddingTop: 30,
      display: 'flex',
      width: '100%',
      height: '100%',
      flexWrap: 'wrap'
    },
  }));

export default style 